import { createContext } from "react";

// we use createContext to create a context object
// context is used to share data between components without passing props manually at every level

const UserContext = createContext({
     
})

export default UserContext;